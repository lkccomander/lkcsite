Now I have everything I need. This is a rich dataset with some very clear signals. Let me build the insight dashboard.Here's what the raw telemetry actually tells you, cutting through the NIM review where it was wrong.

**The most important finding: your filters are working correctly.** The shadow PnL analysis is unambiguous — the 1,007 trades your bot rejected had a 89.5% would-lose rate and a total hypothetical PnL of **−$719**. This is the opposite of what the NIM review concluded. NIM said "widen your entry filters." The data says "your filters are your only protection." Do not touch `entry_time_ratio` or `entry_price_ratio` based on NIM's recommendation.

**The real problem is strategy win rate, not filters.** Of 7 executed trades, 2 won and 5 lost (29% win rate). Both wins hit take-profit at 0.87+ and exited fast (23s, 58s). All 3 losses were stop-loss hits at 0.43 — exactly 0.20 below entry. The 2 "forced exits" were the most interesting: trades 3, 6, and 7 exited at 14–15s hold time with prices at 0.52–0.61, suggesting the market moved against you before the 25s forced exit clock even started. That's a fast-reversal pattern worth tracking.

**The NIM review had a data quality issue.** It read the latest single session (which was zero-trade) and concluded `entry_price_ratio` was the dominant rejection at 73%. Across all 6 sessions, the actual dominant rejection is `entry_time_ratio` at 68.6% — every single one of those 1,598 rejections happened when `remainingTime > 195s`. The bot correctly does nothing in the early 65% of each market window, which is by design.

**The feed latency number is concerning.** Average latency across sessions was 2,058ms with a max of 12,928ms. RTT average was 309ms with a spike to 5,233ms. For a 300-second market, a 12-second latency spike is 4% of the entire window. This is worth instrumenting more carefully — trades 6 and 7 both exited in 14s, and it's possible they were entered on stale data.

**The one config change worth testing now:** `min_fee_adjusted_edge = 0.06` (6%) is high relative to what the 2 winning trades actually captured. Trade 1 entered at 0.60, exited at 0.87 — that's 27 cents of price movement on a $5 position, a 44% gross return. The fee was $0.14 (2.8%). The edge filter at 6% wasn't the binding constraint for these wins. But for the 5 losing trades, no edge requirement would have saved you — those were directional bets that went wrong. The question for the next experiment phase is whether the 29% win rate has a signal underneath it, or whether trade_4 is selecting randomly within its narrow price window.